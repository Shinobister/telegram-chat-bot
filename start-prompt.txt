Create a universal, modular Python script that uses the python-telegram-bot library to listen for incoming Telegram messages. The project name is the session name Claude assigns to this chat, so every message must be prefixed by that project name (e.g., 'project-alpha') to identify which project is active. Forward user messages to a language model (like Claude) via API, and send back the model’s response. Structure the script so that listening, sending, and responding components are modular and reusable. In the same project folder, create a Markdown file that will live alongside this script. In that file, explicitly note the exact path of the Python script so Claude knows where it is located when it runs. Include detailed instructions in this Markdown file so Claude knows how to use it—this is how Claude should communicate when the user is away, and specify a keyword that, when typed, terminates the script, so it stops listening. After each interaction, have the script periodically check if total messages exceed 50, and if so, compress them into a summary within the Markdown history section, while keeping the most recent 20 messages separate. The result of this project should be a Python script with comments inside that explain how to use it, along with a Markdown template file that I will copy into the project folder, and this file will serve as the guide for Claude to handle communication and history storage.